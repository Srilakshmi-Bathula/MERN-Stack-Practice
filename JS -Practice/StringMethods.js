/*
string methods : allow you to manipulate and work with text (strings)
*/
let str = "SriTech";

//str = str.length;
//str = str.toUpperCase();
//str = str.toLowerCase();
//str = str.charAt(0);
//str = str.indexOf("S");
//str = str.lastIndexOf("i");
str = str.padEnd(10, 0);
console.log(str);
